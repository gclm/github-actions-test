name: Actions Step1

on:
  push:
    branches:
      - main
      - 'releases/**'
  workflow_dispatch:

env:
  UPLOAD_RELEASE: true
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 检查项目分支
        uses: actions/checkout@main

      - name: 获取最新的tag标签
        id: previousTag
        run: |
          name=$(git --no-pager tag --sort=creatordate | tail -2 | head -1)
          echo "previousTag: $name"
          echo "previousTag=$name" >> $GITHUB_ENV

      - name: 生成release标签
        id: tag
        run: |
          git --no-pager tag --sort=-creatordate
          release_tag=redmi-ax6000-v$(date +"%Y%m%d%H%M")
          release_title="🎉🎉 红米AX6000: $release_tag 发布成功"
          git tag -a $release_tag -m  "$release_title"
         
          echo "当前生成release标签：$release_tag"
          echo "当前最新标签：$(git --no-pager tag --sort=-creatordate | head -n 1)"
          echo "release_tag=$release_tag" >> $GITHUB_OUTPUT
          echo "release_title=$release_title" >> $GITHUB_OUTPUT

      - name: Update CHANGELOG
        id: changelog
        uses: requarks/changelog-action@v1
        with:
          token: ${{ github.token }}
          fromTag: ${{ steps.tag.outputs.release_tag }}
          toTag: ${{ env.previousTag }}
          writeToFile: false

      - name: 上传firmware到Release
        uses: softprops/action-gh-release@master
        if: env.UPLOAD_RELEASE == 'true'
        with:
          tag_name: ${{ steps.tag.outputs.release_tag }}
          name: ${{ steps.tag.outputs.release_title }}
          body: ${{ steps.changelog.outputs.changes }}
          files: |
            ${{ env.FIRMWARE }}/*


  delete-workflow:
    needs: [ build ]
    runs-on: ubuntu-latest
    steps:
      - name: 删除workflow运行
        uses: Mattraks/delete-workflow-runs@main
        with:
          retain_days: 0
          keep_minimum_runs: 2
